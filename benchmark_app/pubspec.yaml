name: benchmark_app
description: "A benchmark app for dentity" 

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  dentity:
    path: ..

  fl_chart: ^0.69.0
  file_picker: ^8.1.4
  desktop_drop: ^0.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true


